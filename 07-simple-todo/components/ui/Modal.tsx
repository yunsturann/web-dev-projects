"use client";

import { useState } from "react";

const Modal = () => {
  const [showModal, setShowModal] = useState<boolean>(false);

  return (
    <div>
      <button></button>
    </div>
  );
};

export default Modal;
